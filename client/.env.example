# ---------------------------------------------------------------------------
# PulseKeeper — frontend configuration
#
# Copy to client/.env. Only VITE_-prefixed variables reach the browser bundle,
# and everything here is public by definition: never put a secret in this file.
# ---------------------------------------------------------------------------

# Base URL of the PulseKeeper API.
VITE_API_URL=http://localhost:5050
